#include <chargementMemoireExterne.h>

/**
 * @brief Programme de chargement en mémoire externe des données
 * 
 * @return int 
 */

int main()
{
    ChargementMemoireExterne charge;
    charge.ecritureDonnees();
    return 0;
}